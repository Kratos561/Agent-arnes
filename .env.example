# This static application reads provider settings from your browser's localStorage.
# Do not commit provider keys or create a .env file for GitHub Pages.
# Configure your API key from the Settings dialog after opening the app.
